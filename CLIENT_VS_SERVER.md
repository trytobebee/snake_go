# 📁 客户端 vs 服务器端代码分类

## 🎯 快速识别

### 客户端代码 (浏览器运行)
- **语言**: HTML, CSS, JavaScript
- **位置**: `web/static/` 目录
- **运行**: 用户的浏览器
- **标识**: `.html`, `.css`, `.js` 文件

### 服务器端代码 (Go程序运行)
- **语言**: Go
- **位置**: `cmd/`, `pkg/` 目录
- **运行**: 你的Mac/服务器
- **标识**: `.go` 文件

---

## 📂 完整文件分类

```
snake_go/
│
├── 🌐 客户端代码 (浏览器)
│   └── web/static/
│       ├── index.html          # 页面结构
│       ├── style.css           # 视觉样式
│       └── game.js             # 客户端逻辑
│
├── 🖥️ 服务器端代码 (Go)
│   ├── cmd/
│   │   ├── snake/main.go       # 终端版入口
│   │   └── webserver/main.go   # Web服务器入口
│   │
│   └── pkg/
│       ├── game/               # 游戏核心逻辑
│       │   ├── types.go
│       │   ├── game.go
│       │   ├── food.go
│       │   ├── game_test.go
│       │   └── bonus_test.go
│       ├── renderer/           # 渲染器
│       │   ├── terminal.go
│       │   └── terminal_bench_test.go
│       ├── input/              # 输入处理
│       │   └── keyboard.go
│       └── config/             # 配置
│           └── config.go
│
└── 📄 其他文件
    ├── go.mod                  # Go依赖管理
    ├── go.sum                  # 依赖校验
    ├── build.sh                # 构建脚本
    ├── README.md               # 项目说明
    └── *.md                    # 文档
```

---

## 详细分类表

### 🌐 客户端代码 (3个文件)

| 文件 | 类型 | 作用 | 运行位置 |
|------|------|------|---------|
| `web/static/index.html` | HTML | 页面结构和布局 | 浏览器 |
| `web/static/style.css` | CSS | 样式、动画、美化 | 浏览器 |
| `web/static/game.js` | JavaScript | WebSocket通信、Canvas渲染、键盘监听 | 浏览器 |

**这些文件**:
- ✅ 被浏览器下载
- ✅ 在浏览器中执行
- ✅ 用户可以看到源代码 (F12查看)
- ✅ 负责显示和用户交互

---

### 🖥️ 服务器端代码 (Go文件)

#### 1. 程序入口 (2个)

| 文件 | 用途 | 编译产物 | 运行方式 |
|------|------|---------|---------|
| `cmd/snake/main.go` | 终端版游戏 | `snake` 二进制 | `./snake` |
| `cmd/webserver/main.go` | Web服务器 | `webserver` 二进制 | `./webserver` |

#### 2. 共享核心逻辑 (pkg/)

##### pkg/game/ - 游戏核心
| 文件 | 作用 | 使用者 |
|------|------|--------|
| `types.go` | 数据结构定义 (Point, Food, Game) | 所有模块 |
| `game.go` | 游戏主逻辑 (移动、碰撞、更新) | 终端版 + Web版 |
| `food.go` | 豆子相关逻辑 | 游戏核心 |
| `game_test.go` | 暂停功能测试 | 测试 |
| `bonus_test.go` | 位置奖励测试 | 测试 |

##### pkg/renderer/ - 渲染器
| 文件 | 作用 | 使用者 |
|------|------|--------|
| `terminal.go` | 终端渲染 | 终端版 |
| `terminal_bench_test.go` | 性能测试 | 测试 |

##### pkg/input/ - 输入处理
| 文件 | 作用 | 使用者 |
|------|------|--------|
| `keyboard.go` | 键盘输入处理 | 终端版 |

##### pkg/config/ - 配置
| 文件 | 作用 | 使用者 |
|------|------|--------|
| `config.go` | 游戏常量配置 | 所有模块 |

---

## 🔍 代码执行位置对比

### Web版本执行流程

```
客户端 (浏览器)                     服务器端 (Go)
────────────────────                ────────────────────

📄 index.html
  ├─ 定义页面结构                   
  └─ 加载 CSS 和 JS                 

🎨 style.css
  └─ 应用样式和动画                 

⚡ game.js                          📝 cmd/webserver/main.go
  ├─ new WebSocket()  ──────连接───→ ├─ upgrader.Upgrade()
  ├─ setupKeyboard()                │ └─ 建立WebSocket
  │  └─ 监听键盘                     │
  ├─ ws.send()  ─────按键数据──→   │
  │                                  ├─ NewGameServer()
  │                   ←─游戏状态───  ├─ handleAction()
  ├─ ws.onmessage                   │
  │  └─ 收到游戏状态                 │ 📦 pkg/game/
  ├─ updateUI()                     │   ├─ game.go
  │  └─ 更新分数、速度               │   │  └─ Update()
  └─ render()                       │   │     ├─ 蛇移动
     ├─ 清空Canvas                  │   │     ├─ 检测碰撞
     ├─ 画墙壁                       │   │     └─ 吃豆子
     ├─ 画豆子                       │   └─ food.go
     └─ 画蛇                         │      └─ 豆子逻辑
                                     │
                                     └─ ticker循环
                                        └─ 每16ms发送状态
```

### 终端版本执行流程

```
终端版 (单机)
────────────────────

📝 cmd/snake/main.go
  ├─ NewGame()
  ├─ keyboard监听
  ├─ ticker循环
  └─ 本地完成所有逻辑

📦 pkg/game/
  ├─ game.go (游戏逻辑)
  └─ food.go (豆子逻辑)

🎨 pkg/renderer/terminal.go
  └─ Render() (终端输出)

⌨️ pkg/input/keyboard.go
  └─ 读取键盘输入

一切都在本地运行 ✓
无需网络通信 ✓
```

---

## 🎯 关键区别

### Web版本的分工

```
客户端职责:
✅ 显示界面 (HTML/CSS)
✅ 渲染游戏画面 (Canvas)
✅ 监听键盘 (JavaScript)
✅ 发送按键到服务器
✅ 接收游戏状态
✅ 更新UI显示

❌ 不运行游戏逻辑
❌ 不计算蛇的位置
❌ 不检测碰撞
❌ 不生成豆子

────────────────────

服务器职责:
✅ 运行游戏逻辑 (pkg/game/)
✅ 计算蛇的移动
✅ 检测碰撞
✅ 生成豆子
✅ 接收客户端输入
✅ 发送游戏状态

❌ 不渲染图形
❌ 不处理CSS样式
❌ 不直接访问用户键盘
```

---

## 📊 代码量统计

### 客户端代码
```
web/static/index.html    ~110 行  (HTML)
web/static/style.css     ~360 行  (CSS)
web/static/game.js       ~266 行  (JavaScript)
────────────────────────────────
总计                      ~736 行
```

### 服务器端代码
```
cmd/webserver/main.go    ~200 行  (Go)
pkg/game/*.go            ~500 行  (Go)
pkg/renderer/*.go        ~200 行  (Go)
pkg/input/*.go           ~90 行   (Go)
pkg/config/*.go          ~30 行   (Go)
────────────────────────────────
总计                    ~1020 行
```

---

## 🔑 识别技巧

### 快速判断文件类型

1. **看文件扩展名**
   - `.html`, `.css`, `.js` → 客户端
   - `.go` → 服务器端

2. **看文件位置**
   - `web/static/` → 客户端
   - `cmd/`, `pkg/` → 服务器端

3. **看import语句**
   ```javascript
   // JavaScript (客户端)
   document.getElementById(...)
   new WebSocket(...)
   canvas.getContext('2d')
   ```
   
   ```go
   // Go (服务器端)
   import "github.com/gorilla/websocket"
   import "net/http"
   ```

4. **看运行方式**
   - 浏览器中运行 → 客户端
   - `go run`/二进制运行 → 服务器端

---

## 📝 总结表

| 特性 | 客户端 | 服务器端 |
|------|--------|---------|
| **语言** | HTML/CSS/JS | Go |
| **位置** | `web/static/` | `cmd/`, `pkg/` |
| **运行** | 浏览器 | Mac/服务器 |
| **文件数** | 3个 | ~15个 |
| **代码量** | ~700行 | ~1000行 |
| **职责** | 显示+交互 | 游戏逻辑 |
| **可见性** | 用户可查看源码 | 用户看不到 |
| **编译** | 不需要 | 需要 (`go build`) |
| **修改后** | 刷新浏览器 | 重启服务器 |

---

## 🎮 实际例子

### 示例1: 按↑键

```
客户端 (game.js):
document.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowUp') {
        ws.send(JSON.stringify({action: 'up'}));  // 发送
    }
});

服务器 (cmd/webserver/main.go):
func handleAction(action string) {
    if action == "up" {
        game.SetDirection(Point{X:0, Y:-1})  // 处理
    }
}
```

### 示例2: 渲染蛇

```
客户端 (game.js):
gameState.snake.forEach(segment => {
    ctx.fillRect(segment.x * 20, segment.y * 20, 18, 18);  // 画
});

服务器 (pkg/game/game.go):
func (g *Game) Update() {
    newHead := Point{
        X: head.X + g.Direction.X,  // 计算
        Y: head.Y + g.Direction.Y,
    }
    g.Snake = append([]Point{newHead}, g.Snake...)  // 更新
}
```

---

现在清楚了吗？

**简单记忆**:
- 📁 `web/static/` = 客户端 (浏览器)
- 📁 `cmd/` + `pkg/` = 服务器端 (Go)
- 🌐 HTML/CSS/JS = 客户端
- 🖥️ `.go` 文件 = 服务器端

🎯 核心原则：**看到就运行 → 客户端；编译后运行 → 服务器端**
